plugins {
    id 'com.android.application'
}

android {
    namespace 'com.dani.hours'
    compileSdk 34

    defaultConfig {
        applicationId 'com.dani.hours'
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName '1.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            // Signed with the build's debug key so the APK installs directly by sideloading.
            signingConfig signingConfigs.debug
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
